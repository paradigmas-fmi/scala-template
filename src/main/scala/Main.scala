object Main {
  def main(args: Array[String]): Unit = {
    // TP1 MonaLambda: leer líneas por stdin (definiciones, calc_vars, reduce, …).
    // Implementación del intérprete a cargo del grupo.
    scala.io.Source.stdin.getLines().foreach(_ => ())
  }
}
